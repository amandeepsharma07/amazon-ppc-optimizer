# The only setting the app needs.
#
# Free hosted Postgres (no install): make a project at neon.tech and paste the
# connection string it gives you.
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# Or a Postgres running on your own machine:
# DATABASE_URL=postgres://postgres:yourpassword@localhost:5432/ppcopt
